name: "test-extension"

page_actions:
  - quotations:
      actions:
      - identifier: "dialog"
        page: "quotation"
        executable: 
          type: "dialog"
          entrypoint: "dialog.html"

table_extensions:
  - quotations:
      actions:
      - name: "confirm_quotation"
        executable:
          type: "dialog"
          entrypoint: "confirm_dialog.html"
      columns:
        - name: "delivery-date"
          title: "Delivery date"
          position: "quotation"
          entrypoint: "delivery-date.html"
